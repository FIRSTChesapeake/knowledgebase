Upload a pre-roll and/or post-roll video from the Branding page (admin only). Once set, the
platform automatically splices them onto the start/end of clips it publishes for your Client — no
per-clip action needed.

An individual event can also override the pre-roll for just that event from its own configuration
page; see
[[Event-Streaming-Management/Client-Administration/6. Event Configuration Reference|Event Configuration Reference]].
